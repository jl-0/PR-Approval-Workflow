[project]
name = "aurora-telemetry-gateway"
version = "0.4.0"
description = "Ingest, decode, and serve spacecraft telemetry measurands."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
